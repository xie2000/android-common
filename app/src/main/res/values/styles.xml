<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <declare-styleable name="ExpandTextView">
        <attr name="etv_lines" format="integer" />
        <attr name="etv_shrinkDrawable" format="reference" />
        <attr name="etv_expandDrawable" format="reference" />
        <attr name="etv_textStateColor" format="color" />
        <attr name="etv_textStateSize" format="dimension" />
        <attr name="etv_textContentColor" format="color" />
        <attr name="etv_textContentSize" format="dimension" />
        <attr name="etv_textShrink" format="string" />
        <attr name="etv_textExpand" format="string" />
    </declare-styleable>

    <style name="my_text">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="loading_dialog" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="xloading_padding_style">
        <item name="android:paddingTop">@dimen/spacing</item>
        <item name="android:paddingBottom">@dimen/spacing</item>
        <item name="android:paddingLeft">@dimen/XFrame_spacing_large</item>
        <item name="android:paddingRight">@dimen/XFrame_spacing_large</item>
    </style>


    <!-- 自定义仿ListDialogIOS底部Dialog的样式 ，有模糊效果 -->
    <style name="ListDialogIOSStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ListDialogAnimation</item>
    </style>

    <!-- ListDialogIOS进出动画 -->
    <style name="ListDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/ani_list_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/ani_list_dialog_out</item>
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogIOSStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 自定义仿IOS的AlertDialog的样式,无阴影背景 -->
    <style name="AlertDialogIOSStyle_noshadow" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
